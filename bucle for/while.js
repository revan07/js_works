numeros = 0;

while(numeros < 50){

    /* "numeros++" sirve para sumar o
     mejor dicho cortar cada vez que while pregunte si numeros < 50 (si es menor que 50) */
    numeros++;
    document.write(numeros + "<br>");
    if(numeros == 30){
        break;
    }
}

//primero hace el codigo y lo muestra por pantalla
/* numero = 0;

do {
document.write(numero + "<br>");

    numero++
}
while(numero <10)

 */

//te lo seguira preguntando siempre asta que se detenga cumpliendose o terminando la operacion
/* let num = 0;

while (num <10 ){
num++;
document.write(num);

}
 */